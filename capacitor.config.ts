import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ro.uoradea.cti.padmw.ionic.smartreporter',
  appName: 'IonicSmartReporter',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
